version: 2

sources:
  - name: stripe
    description: A clone of a Postgres database
    database: dbt-tutorial
    schema: stripe
    config:
      freshness:
        warn_after:
          count: 12
          period: hour
        error_after:
          count: 1
          period: day
      loaded_at_field: _batched_at
    tables:
      - name: payment
        description: Raw payment data
        columns:
          - name: id
            description: Primary key
            data_tests:
              - unique
              - not_null
          - name: orderid
            description: Foreign key, Primary key of source Jaffle_shop Orders
            data_tests:
              - not_null
              - relationships:
                  to: ref('stg_jaffle_shop__orders')
                  field: order_id
          - name: paymentmethod
            description: "{{ doc('payments__paymentmethod') }}"
            data_tests:
              - accepted_values:
                  values: ['bank_transfer', 'coupon', 'credit_card', 'gift_card']
          - name: status
            description: "{{ doc('payments__status') }}"
            data_tests:
              - accepted_values:
                  values: ['fail', 'success']
          - name: created
            data_tests:
              - not_null